<!DOCTYPE html>
<html>
  <head>
    <title>BudgetTracker</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" rel="stylesheet">

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <% if user_signed_in? %>
      <nav class="flex justify-between h-14 bg-sky-600 items-center px-5 mb-4">
        <button id="hamburger-button" class="text-white hover:text-gray-700 focus:outline-none">
          <i class="fas fa-bars fa-2x"></i>
        </button>
        <% if controller_name == 'category' && action_name == 'index' %>
          <h1 class="text-white text-xl ml-4">Categories</h1>
        <% else %>
          <h1 class="text-white text-xl ml-4">Bills</h1>
        <% end %>
        <%= link_to "Sign Out", destroy_user_session_path, class: "text-white text-sm" %>
      </nav>
    <% end %>

    <main class="container mx-auto h-full items-center">
      <div id="flash-messages">
        <small><%= notice %></small>
        <small><%= alert %></small>
      </div>
      <%= yield %>
    </main>
  </body>
</html>
